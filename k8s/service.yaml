apiVersion: v1
kind: Service
metadata:
  name: hello-python-service
  labels:
    app: hello-python
spec:
  type: NodePort
  selector:
    app: hello-python
  ports:
    - name: http
      port: 32777
      targetPort: 32777
      protocol: TCP
